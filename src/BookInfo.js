import { Card, CardContent } from '@mui/material';

function BooksInfo({ books }) {
return (
<div className='books-container'>
<Card className='books-card'>
{/* ... (rest of your BooksInfo component JSX) */}
</Card>
</div>
);
}

export default BooksInfo;